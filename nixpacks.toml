# Nixpacks build configuration for Railway.
# Adds ffmpeg alongside Python so yt-dlp can convert downloaded audio to mp3.
# python311 must be listed explicitly — defining [phases.setup] overrides auto-detection.
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]
